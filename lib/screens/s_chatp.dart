import 'package:flutter/material.dart';

// ignore: camel_case_types
class s_chatp extends StatelessWidget {
  const s_chatp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Seller Chat Page')),
    );
  }
}
