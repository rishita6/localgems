import 'package:flutter/material.dart';
import 'chatlistpage.dart';

class s_chatp extends StatelessWidget {
  const s_chatp({super.key});

  @override
  Widget build(BuildContext context) {
    return const ChatListPage(); // Uses common chat list
  }
}
